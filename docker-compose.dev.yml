services:
  dev:
    build:
      context: .
      dockerfile: deploy/Dockerfile.dev
    container_name: xnet-dev
    hostname: xnet-dev
    stdin_open: true
    tty: true
    network_mode: host
    volumes:
      - /workspace:/workspace
    restart: unless-stopped
